procedure Clause2 {
  x = 1;
  y = 2;
  read z;
  if (x > z) then {
    print x; 
    while (i < 10) {
      i = i + 2; } }
  else {
    print z;
    if (y > z) then {
      print y; }
    else {
      z = y + x; } }
  read first;
  read second;
  while (first < second) {
    read add;
    while (add > 5) {
      add = 2;
      print add; }
    first = first + add; }
  print first;
  print second;
}